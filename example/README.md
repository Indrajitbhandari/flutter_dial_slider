# example

Example app for the `dial_slider` package.

## Getting Started

Run it from the repo root:

```bash
cd example
flutter pub get
flutter run
```

The home screen renders `DialSlider` and shows the currently selected value.
